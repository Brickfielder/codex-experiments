---
import OngoingStudiesPage from '../../components/pages/OngoingStudiesPage.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'fr' } },
    { params: { lang: 'es' } },
    { params: { lang: 'it' } },
  ];
}

const { lang } = Astro.params;
---

<OngoingStudiesPage lang={lang} />
