---
import HeatmapPage from '../../components/pages/HeatmapPage.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'fr' } },
    { params: { lang: 'es' } },
    { params: { lang: 'it' } },
  ];
}

const { lang } = Astro.params;
---

<HeatmapPage lang={lang} />
